var game=UnityEngine.Object.FindAnyObjectByType<LittleSwitch.ShopGame>();if(!game)throw new System.Exception("Play required");var flags=System.Reflection.BindingFlags.Instance|System.Reflection.BindingFlags.NonPublic;var type=game.GetType();var state=game.state;var save=System.IO.File.ReadAllBytes(LittleSwitch.ShopSave.PathName);var close=game.closeView;var cam=game.viewCamera;var cp=cam.transform.position;var cr=cam.transform.rotation;var packages=UnityEngine.Object.FindObjectsByType<LittleSwitch.PartsPackage>(UnityEngine.FindObjectsSortMode.None);var results=new System.Collections.Generic.List<string>();
void Run(System.Collections.IEnumerator e){int count=0;try{while(e.MoveNext()){if(++count>10000)throw new System.Exception("Animation limit");if(e.Current is System.Collections.IEnumerator child)Run(child);}}finally{(e as System.IDisposable)?.Dispose();}}
try{
 game.state=UnityEngine.JsonUtility.FromJson<LittleSwitch.BuildState>(UnityEngine.JsonUtility.ToJson(state));
 cam.transform.position=new UnityEngine.Vector3(1.8f,8,-10.6f);cam.transform.LookAt(new UnityEngine.Vector3(0,5.2f,1));
 foreach(var p in packages){p.Present(false);UnityEngine.Physics.SyncTransforms();var target=p.GetComponent<UnityEngine.Collider>().bounds.center;var ray=cam.ScreenPointToRay(cam.WorldToScreenPoint(target));if(!UnityEngine.Physics.Raycast(ray,out var hit,100)||hit.collider.GetComponentInParent<LittleSwitch.PartsPackage>()!=p)throw new System.Exception("Package occluded "+p.name);game.state.stage=p.keycaps?LittleSwitch.BuildStage.Keycaps:LittleSwitch.BuildStage.Switches;Run((System.Collections.IEnumerator)type.GetMethod("OpenPackage",flags).Invoke(game,new object[]{p}));if(!p.IsOpen||UnityEngine.Vector3.Distance(p.transform.position,p.deskPosition)>.001f||p.GetComponent<UnityEngine.Collider>().enabled)throw new System.Exception("Opening failed");if(p.keycaps?!game.state.capPackageOpened:!game.state.switchPackageOpened)throw new System.Exception("Package persistence flag failed");results.Add(p.name+" pickup/open pass");}
 cam.transform.position=new UnityEngine.Vector3(-1.15f,9.6f,-2.8f);cam.transform.LookAt(new UnityEngine.Vector3(-1.15f,4.15f,-.55f));int hits=0;UnityEngine.Physics.SyncTransforms();for(int i=0;i<61;i++){var slots=game.board.GetComponentsInChildren<LittleSwitch.KeySlot>().Where(s=>s.index==i).ToArray();var target=slots[slots.Length-1].GetComponent<UnityEngine.Collider>().bounds.center;if(UnityEngine.Physics.Raycast(cam.ScreenPointToRay(cam.WorldToScreenPoint(target)),out var hit,100)&&hit.collider.GetComponentInParent<LittleSwitch.KeySlot>()?.index==i)hits++;}if(hits!=61)throw new System.Exception("Key hits "+hits);results.Add("61 key targets clear");
 var drink=UnityEngine.Object.FindAnyObjectByType<LittleSwitch.HotDrink>();var pos=drink.transform.position;var rot=drink.transform.rotation;int n=drink.Sips;Run(drink.Sip(cam));if(drink.IsSipping||drink.Sips!=n+1||UnityEngine.Vector3.Distance(pos,drink.transform.position)>.001f||UnityEngine.Quaternion.Angle(rot,drink.transform.rotation)>.01f)throw new System.Exception("Drink return failed");results.Add("Sip and return pass");
 var music=game.GetComponents<UnityEngine.AudioSource>().FirstOrDefault(a=>a.clip&&a.clip.name=="ChillLofi");if(!music||!music.isPlaying||!music.loop||music.clip.length<100)throw new System.Exception("Music not playing");results.Add("Lo-fi playing, loop enabled, "+music.clip.length+" seconds");
}finally{game.state=state;game.closeView=close;cam.transform.SetPositionAndRotation(cp,cr);type.GetField("busy",flags).SetValue(game,false);type.GetMethod("RestorePackages",flags).Invoke(game,null);type.GetMethod("RefreshSupply",flags).Invoke(game,null);game.ui.Refresh();System.IO.File.WriteAllBytes(LittleSwitch.ShopSave.PathName,save);}
results.Add("Player save restored byte-for-byte");return results;
